server:
  port: 8080

spring:
  application:
    name: s3
  zipkin:
    base-url: http://127.0.0.1:9411/
  sleuth:
    sampler:
      percentage: 1.0

logging:
  level.com.example.demo.jmskafka: DEBUG
  config: classpath:logging/logback-spring.xml

